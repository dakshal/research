Crime forecasting using RNN and CNN models

to compile the code install python to your PC and then install all other dependent library using pip install

then try to run the code by python dependency.py
